/**
 * Interface describing service's channel ids
 */
export default interface ServiceChannelIds { 
  electronic: string[],
  phone: string[],
  printableForm: string[],
  webpage: string[],
  serviceLocation: string[]
}